<!-- 课程介绍 -->

<view wx:for="{{kc_list}}">
  <view class='curriculum' data-id='{{item.id}}' bindtap='curriculum_content'>
    <image src='{{url}}{{item.src}}'></image>
    <view>
      <view>{{item.mingcheng}}</view>
      <view>{{item.jianjie}}</view>
    </view>
  </view>
</view>
